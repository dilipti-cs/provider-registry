# IMPORTANT: This is a template. Do NOT commit actual secrets to Git.
# Create actual secrets using: kubectl create secret generic provider-registry-secrets --from-literal=...
# Or use Google Secret Manager with Workload Identity

apiVersion: v1
kind: Secret
metadata:
  name: provider-registry-secrets
  namespace: provider-registry
type: Opaque
stringData:
  # PostgreSQL Credentials - CHANGE THESE IN PRODUCTION
  POSTGRES_USER: "medplum"
  POSTGRES_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  DATABASE_USERNAME: "medplum"
  DATABASE_PASSWORD: "CHANGE_ME_IN_PRODUCTION"

  # Add additional secrets as needed
  # JWT_SECRET: "generate-a-strong-random-secret"
  # ENCRYPTION_KEY: "generate-another-strong-secret"
